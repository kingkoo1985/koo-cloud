server:
  port: 8011
spring:
  application:
    name: koo-cloud-server-monitor
info:
  owner: kingkoo1985@gmail.com
  version: 1.0.0
  app.name: koo-cloud
  company.name: www.koo-cloud.cn
  build.artifactId: $project.artifactId$
  build.version: $project.verson$     
#logging 配置信息
logging:
  level:
    root: info
  path: logs/
  file: ${spring.application.name}.log 